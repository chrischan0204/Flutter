import 'package:flutter/material.dart';

class UserInviteDetailsView extends StatelessWidget {
  const UserInviteDetailsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

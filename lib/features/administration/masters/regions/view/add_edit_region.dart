import 'package:flutter/material.dart';

class AddEditRegionView extends StatelessWidget {
  final String? regionId;
  const AddEditRegionView({
    super.key,
    this.regionId,
  });

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
